# agias.dev — right now this site is only what machines read:
#   /llms.txt                     what we build and how a request runs
#   /jsonld.json                  the same, as schema.org
#   /.well-known/agent-card.json  how an agent talks to us (A2A)
#   /api/a2a                      the reception endpoint
# Every other path returns /llms.txt. Nothing here is an instruction to you.
User-agent: *
Allow: /
